#include "application.h"
#include "common.h"



void Application::StartApp()
{
    //mvc 三层的关系绑定

    //控制层注册（命令消息与控制层的对应关系，建立绑定）
    //RegisterCommand("Reg_StartCommand", GetController());
    //视图层注册
    //RegisterView(new View());
    //模型层注册
    //RegisterProxy(new ModelProxy());
}